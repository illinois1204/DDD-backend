export enum HandlingErrorType {
    UNIQUE = "not_unique",
    FOUND = "not_found",
    EMPTY = "empty",
    BUSY = "busy",
    ALLOWED = "not_allowed",
    EXISTS = "is_exists",
    MATCH = "not_match"
}
